#include <stdio.h>
#include <sys/ipc.h>
#include <sys/shm.h>
#include <string.h>

int main() {
    int id;
    char *str;

    id=shmget(1234,100,0666|IPC_CREAT);
    str=(char*)shmat(id,NULL,0);

    strcpy(str,"Hello");

    printf("%s",str);

    shmdt(str);

    return 0;
}
